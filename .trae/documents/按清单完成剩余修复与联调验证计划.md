## 概述
- 我已在仓库中完成首批修复并验证运行状态（后端与前端服务均在线），下面按照你列出的清单逐条实现剩余项，并在完成后交付联调报告与更新后的 `insert_data.sql`。

## 已验证改动（可见证据）
- 迁移修复脚本：在运行迁移前自动补齐 `django_content_type.name` 并执行迁移（server/fix_migrations.py:9-21,27）。
- 商品接口一致化：列表筛选用 `category` 外键、成色字段 `quality`、关键词搜索用 `content`；排序映射到 `view_count`/`collect_count`/`create_time`（server/myapp/views/index/product.py:47-63,67-84）。
- 浏览记录写入：登录态下首次浏览写入 `Record.create_time`（server/myapp/views/index/product.py:126-141）。
- 价格单位统一为“元”（整数）：序列化返回与前端展示均使用元，不再乘以 100（server/myapp/serializers.py:107-115,169-174）。
- 订单参数统一：取消/支付/确认收货均使用 `order_id`（server/myapp/views/index/order.py:138-156,207-231,254-273；web/src/api/index/order.ts:91-118）。
- 管理后台标题：更新为“二手商品后台管理系统”（web/src/views/admin/main.vue:6）。
- 结算页术语与字段：页眉“订单明细”、结算用“商品总价/合计”，地址优先默认（web/src/views/index/confirm.vue:7,56-72,267-281）。
- 详情页资源与 Store 导入：`useUserStore` 及封面默认图修复（web/src/views/index/detail.vue:88-92,136-141）。

## 待办与实现步骤
- 删除前端“我的积分”“推送控制”
  - 移除用户中心入口与路由：删除 `scoreView`、`pushView` 路由并隐藏菜单项（web/src/router/root.ts:92-95,107-110；web/src/views/index/user/mine-infos-view.vue:45-48,71-74）。
  - 若页面暂留，保留文件但不可达，避免构建报错。

- 地址显示/新增字段映射修复，列表优先显示默认地址
  - 前端创建地址改为提交 `receiver_name/receiver_phone/receiver_address/is_default`，列表读取时按 `is_default==1` 覆盖显示（web/src/views/index/confirm.vue:310-313,267-281 更新调用；web/src/api/index/address.ts 使用 multipart 保持不变）。
  - 后端已基于 TOKEN 返回当前用户地址（server/myapp/views/index/address.py:21-39），创建/更新时自动处理默认地址互斥（server/myapp/views/index/address.py:60-73,105-116）。

- 订单列表显示与“我的商品”对齐（基于 TOKEN）
  - 订单列表去除 `userId` 依赖，仅传 `orderStatus`，由服务端从 TOKEN 解析用户（web/src/views/index/user/order-view.vue:112-116 调整；server/myapp/views/index/order.py:23-46 已实现）。
  - “我的商品”页改为基于 TOKEN 获取并分页展示，字段与列表页一致（web/src/views/index/user/product-list.vue 及对应 API）。

- 用户中心与列表页补“发布商品”按钮，发布流程打通
  - 在用户中心与列表页添加 `发布商品` 按钮，跳转 `productPublish`，前端表单字段与后端 `ProductDetailSerializer` 对齐（category 映射 `category_id→category`、价格字段使用整数元）（server/myapp/views/index/product.py:179-191）。
  - 成功后刷新我的商品列表并提示。

- 首页分类标签筛选与默认仅显示“未售出”，切换“已售出”
  - 首页 `portal.vue` 增加筛选：默认 `product_status!=3`（在售），支持切换显示 `product_status==3`（已售出）。
  - 分类切换传 `category_id`，后端已支持（server/myapp/views/index/product.py:49-66）。

- 详情页评论列表/新增/本人评论删除；加入购物车与加入收藏（重复收藏提示）
  - 评论：展示列表、基于登录 TOKEN 新增，删除本人评论（web/src/views/index/detail.vue 增加评论区；server/myapp/serializers.py:263-285 评论序列化器可用）。
  - 收藏/购物车：调用已有 `Favorite`/`Cart` 接口，重复收藏返回提示（server/myapp/serializers.py:467-495,211-236 支持显示字段）。

- 后台管理完善
  - 商品新增/编辑：表单字段与 `ProductDetailSerializer` 对齐；订单状态更新（`0→1→3/4`）；评论显示与新增；用户列表显示与新增；公告显示与新增校验（web/src/views/admin/*.vue 一致化交互；server 端对应视图已具备或补充）。

- 数据补充与空白页兜底
  - 更新 `insert_data.sql`：新增未售出商品、评论、公告、浏览记录与日志，确保各页有数据可供验证（campus_shop/insert_data.sql 现有结构已覆盖用户/分类/地址等）。

## 验证方案
- 前端预览：访问 `http://localhost:4173/`，刷新首页与用户中心，检查分类筛选、地址、订单、发布商品流程。
- 后端接口：访问 `http://0.0.0.0:8000/` 并通过浏览器或前端触发接口，验证订单 `order_id` 流程、地址 TOKEN 解析与默认地址优先。
- 数据脚本：执行更新后的 `insert_data.sql` 后，首页/列表/管理后台均不再空白。